#include <stdio.h>

// 1. Add Function Prototypes (Declarations)
void biggest3(void);
void factorial(void);

// 2. Specify 'int' return type for main
int main() {
    biggest3();
    factorial();
    
    return 0; // It's good practice to return 0 on success
}
